import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  bar: {
    shadowColor: 'rgba(0, 0, 0, 0.24)',
    shadowOffset: {
      width: 0,
      height: 0,
    },
    shadowRadius: 20,
    shadowOpacity: 1,
  },
});
